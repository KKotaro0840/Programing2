#include <stdio.h>

int main() {
    double a, b, c, avg;

    printf("3つの実数を入力してください: ");
    scanf("%lf %lf %lf", &a, &b, &c);

    avg = (a + b + c) / 3.0;

    printf("平均 = %f\n", avg);

    return 0;
}
